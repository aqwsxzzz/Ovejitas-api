# Database Configuration
# For local development with Docker
DB_NAME=ovejitas
DB_USER=username
DB_PASS=password
DB_HOST=localhost
DB_PORT=5432

# For production with Supabase (uncomment and update values)
# Use Session Pooler for persistent servers (Render, VMs, containers)
# The Session Pooler hostname is natively IPv4-compatible
# DB_NAME=postgres
# DB_USER=postgres.YOUR_PROJECT_REF
# DB_PASS=YOUR_PASSWORD
# DB_HOST=aws-0-YOUR_REGION.pooler.supabase.com
# DB_PORT=5432

# Optional: Disable SSL for local development only
# DB_SSL_DISABLED=true

# JWT Configuration
JWT_SECRET=your-jwt-secret-here

# Environment
NODE_ENV=development

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:5173,https://your-frontend-domain.com

# Cookie Configuration (optional - for production)
# Set this to your domain for cross-subdomain cookies (e.g., .yourdomain.com)
# Leave empty for same-domain cookies
COOKIE_DOMAIN=

# Server Configuration
PORT=8080
HOST=0.0.0.0