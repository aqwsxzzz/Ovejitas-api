# Database Configuration
# Local development with Docker - use individual variables
DB_NAME=ovejitas
DB_USER=username
DB_PASS=password
DB_HOST=localhost
DB_PORT=5432

# Production with Supabase - use DATABASE_URL (recommended)
# Get this from Supabase Dashboard -> Project Settings -> Database -> Connection String (Session Pooler)
# Copy the connection string exactly as provided by Supabase
# If connection issues occur, you can add query parameters: ?sslmode=require
# Example: postgres://postgres.YOUR_PROJECT_REF:YOUR_PASSWORD@aws-0-YOUR_REGION.pooler.supabase.com:5432/postgres
# DATABASE_URL=your-supabase-session-pooler-connection-string

# JWT Configuration
JWT_SECRET=your-jwt-secret-here

# Environment
NODE_ENV=development

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:5173,https://your-frontend-domain.com

# Cookie Configuration (optional - for production)
# Set this to your domain for cross-subdomain cookies (e.g., .yourdomain.com)
# Leave empty for same-domain cookies
COOKIE_DOMAIN=

# Server Configuration
PORT=8080
HOST=0.0.0.0